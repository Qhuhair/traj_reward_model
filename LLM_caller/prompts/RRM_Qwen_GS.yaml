name: "qwen_gs_step1_visual"
description: "GS Step 1 — 仅视觉识别：元素定位 + 匹配判断，不评分"
template: |
  动作: {action_desc}
  子目标: {subgoal}

  请查看截图中的标注位置，回答以下两个问题（只填空，不要给评分）：

  1. 图中标注的点击位置对应的UI元素是：____
  2. 该元素与动作描述是否一致：____（一致/不一致）
